<!DOCTYPE html>
<html>
  <head>
    <title>Kinmiru</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>

      <% if customer_signed_in? %>
       <p>ログインできています。</p>
       <p><%= link_to "マイページ", customers_my_page_path %></p>
       <p><%= link_to "ユーザーログアウト", destroy_customer_session_path, method: :delete %></p>
      <% else %>
       <p><%= link_to "TOP", root_path %></p>
       <p><%= link_to "ユーザーログイン",  new_customer_session_path %></p>
       <p><%= link_to "管理者ログイン", new_admin_session_path %></p>
      <% end %>
    </header>
    <%= yield %>
  </body>
</html>
